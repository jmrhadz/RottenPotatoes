import React from "react";

export default class Review extends React.Component{
    render(){

        //  This component doesn't currently do much.
        //  TODO: deprecate it and add it to the parent component's JSX
        return(
            
            <h1>Movie Review</h1>
        )
    }
}